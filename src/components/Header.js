//import { Padding } from '@mui/icons-material';
import React from 'react';

const Header = () => (
  <header>
    <h1 style={{paddingTop:'0', marginTop:'0'}}>Expense Tracker</h1>
  </header>
);

export default Header;
